define flow output_filter
  if contains(last_bot_message, "suicide", "self-harm", "kill yourself", "harm") then
    block "self_harm"
  end

  if contains(last_bot_message, "attack", "bomb", "terror", "weapon") then
    block "violence"
  end

  if contains(last_bot_message, "ssn", "social security", "credit card", "password", "phone number") then
    block "pii"
  end

  allow
end
